# Fessel MQTT broker — Pi-internal ONLY. There is no cluster->Pi MQTT
# traffic; the cluster reaches the Pi exclusively over HTTPS. Do not expose
# the broker beyond the Pi: bind to loopback only.
listener 1883 127.0.0.1
allow_anonymous true
persistence false
log_dest stdout
