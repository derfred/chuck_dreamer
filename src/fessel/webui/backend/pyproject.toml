[project]
name = "fessel-webui-backend"
version = "0.1.0"
description = "Fessel webui backend: WHEP URL minting + control-plane forwarders"
requires-python = ">=3.12"
dependencies = [
  "fessel-schemas",
  "fastapi>=0.110",
  "uvicorn[standard]>=0.29",
  "pyjwt>=2.8",
  "httpx>=0.27",
]

[project.scripts]
fessel-backend = "app.main:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.uv.sources]
fessel-schemas = { path = "../../schemas", editable = true }

[dependency-groups]
dev = [
  "pytest>=8.0",
  "httpx>=0.27",
]

[tool.ruff]
line-length = 100
indent-width = 2

[tool.ruff.format]
indent-style = "space"
