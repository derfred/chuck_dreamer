# Generated by tools/generate-types.sh — never hand-edit or reformat. Prettier
# must leave it byte-for-byte as the generator emits it, or `make check-types`
# (the sync guard) would see spurious drift after `npm run format`.
src/generated/
