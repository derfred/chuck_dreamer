// GENERATED by tools/generate-types.sh from src/fessel/schemas — do not edit.
import { z } from "zod";

export const ModeTripletSchema = z.object({ "resolution": z.string().regex(new RegExp("^\\d+x\\d+$")).describe("\"<W>x<H>\", e.g. \"1280x720\""), "fps": z.number().int().gt(0), "bitrate_bps": z.number().int().gt(0) }).describe("A concrete video operating point.\n\n`resolution` is the canonical \"<W>x<H>\" string; fps and bitrate_bps are\nintegers. The full canonical form (incl. fps and bitrate) is produced by\nmode_to_canonical and is what travels in the WHEP `mode` query parameter\nand the signed token.")


export const CapabilitiesSchema = z.object({ "modes": z.array(z.object({ "resolution": z.string().regex(new RegExp("^\\d+x\\d+$")).describe("\"<W>x<H>\", e.g. \"1280x720\""), "fps": z.number().int().gt(0), "bitrate_bps": z.number().int().gt(0) }).describe("A concrete video operating point.\n\n`resolution` is the canonical \"<W>x<H>\" string; fps and bitrate_bps are\nintegers. The full canonical form (incl. fps and bitrate) is produced by\nmode_to_canonical and is what travels in the WHEP `mode` query parameter\nand the signed token.")) }).describe("Retained payload on arm/video/capabilities.")


export const LiveActivateSchema = z.object({ "path": z.string(), "mode": z.object({ "resolution": z.string().regex(new RegExp("^\\d+x\\d+$")).describe("\"<W>x<H>\", e.g. \"1280x720\""), "fps": z.number().int().gt(0), "bitrate_bps": z.number().int().gt(0) }).describe("A concrete video operating point.\n\n`resolution` is the canonical \"<W>x<H>\" string; fps and bitrate_bps are\nintegers. The full canonical form (incl. fps and bitrate) is produced by\nmode_to_canonical and is what travels in the WHEP `mode` query parameter\nand the signed token.") }).describe("Payload of arm/video/cmd/live/activate.")


export const LiveDeactivateSchema = z.object({ "path": z.string() }).describe("Payload of arm/video/cmd/live/deactivate.")


export const LiveStateSchema = z.object({ "state": z.enum(["off","starting","running","stopping"]), "path": z.union([z.string(), z.null()]).default(null), "mode": z.union([z.object({ "resolution": z.string().regex(new RegExp("^\\d+x\\d+$")).describe("\"<W>x<H>\", e.g. \"1280x720\""), "fps": z.number().int().gt(0), "bitrate_bps": z.number().int().gt(0) }).describe("A concrete video operating point.\n\n`resolution` is the canonical \"<W>x<H>\" string; fps and bitrate_bps are\nintegers. The full canonical form (incl. fps and bitrate) is produced by\nmode_to_canonical and is what travels in the WHEP `mode` query parameter\nand the signed token."), z.null()]).default(null) }).describe("Retained payload on arm/video/state/live.")


export const PlugStateSchema = z.object({ "on": z.union([z.boolean(), z.null()]).default(null), "verified": z.boolean().default(false), "verified_at": z.union([z.string(), z.null()]).default(null) }).describe("A WiZ plug's last *verified* state (S3.1/S3.3).\n\n`on` reflects the state read back via getPilot after the last action or\nbackground refresh — not the last command issued. `verified` is False when\nthe most recent verify failed (the command may have been sent but could not\nbe confirmed); `verified_at` is the ISO-8601 timestamp of that read.")


export const CameraStateSchema = z.object({ "up": z.union([z.boolean(), z.null()]).default(null) }).describe("Camera up/down, derived from the retained arm/video/state/* topics.")


export const RecordingStateSchema = z.object({ "state": z.enum(["idle","starting","recording","stopping"]).describe("video's explicit-recording branch state machine (V4.3).\n\nSeparate from the live branch's LiveStateValue: the two branches share the\nencoder hardware but are independent. `idle` is the at-rest state (no\nrecording), mirroring the live branch's `off`.").default("idle"), "active_recording_id": z.union([z.string(), z.null()]).default(null), "started_at": z.union([z.string(), z.null()]).default(null) }).describe("Retained payload on arm/video/state/recording (V4.5).\n\nThe single source of truth for \"is a recording active right now\"; the\ndashboard derives its Start/Stop button from this (via supervisor /state).")


export const RecordingMetadataSchema = z.object({ "id": z.string(), "started_at": z.string(), "ended_at": z.union([z.string(), z.null()]).default(null), "operator": z.union([z.string(), z.null()]).default(null), "mode": z.union([z.object({ "resolution": z.string().regex(new RegExp("^\\d+x\\d+$")).describe("\"<W>x<H>\", e.g. \"1280x720\""), "fps": z.number().int().gt(0), "bitrate_bps": z.number().int().gt(0) }).describe("A concrete video operating point.\n\n`resolution` is the canonical \"<W>x<H>\" string; fps and bitrate_bps are\nintegers. The full canonical form (incl. fps and bitrate) is produced by\nmode_to_canonical and is what travels in the WHEP `mode` query parameter\nand the signed token."), z.null()]).default(null), "duration_seconds": z.union([z.number().int(), z.null()]).default(null), "segments": z.union([z.number().int(), z.null()]).default(null), "flagged_for_upload": z.boolean().default(false), "upload_state": z.enum(["none","queued","uploading","uploaded","failed"]).describe("Where a recording is in the flag-for-upload pipeline (V4.6/V4.7).\n\n`none` — never flagged. `queued` — marker written, uploader not yet acting.\n`uploading` — transfer in progress. `uploaded` — in MinIO, marker deleted.\n`failed` — non-retryable failure; marker renamed `.failed`.").default("none"), "uploaded_at": z.union([z.string(), z.null()]).default(null) }).describe("The per-recording metadata.json written on finalisation (V4.4).\n\n`flagged_for_upload` and `upload_state` are mutable in place: flag-upload\n(V4.5) and the uploader (V4.7) rewrite this file. It is the recording's\nsource of truth for the rest of the system (listing, playback routing).")


export const UploadProgressSchema = z.object({ "recording_id": z.string(), "state": z.enum(["none","queued","uploading","uploaded","failed"]).describe("Where a recording is in the flag-for-upload pipeline (V4.6/V4.7).\n\n`none` — never flagged. `queued` — marker written, uploader not yet acting.\n`uploading` — transfer in progress. `uploaded` — in MinIO, marker deleted.\n`failed` — non-retryable failure; marker renamed `.failed`.").default("queued"), "attempts": z.number().int().default(0), "last_attempt_at": z.union([z.string(), z.null()]).default(null), "last_error": z.union([z.string(), z.null()]).default(null) }).describe("Payload on arm/video/upload/<recording-id> (V4.7), surfaced read-only by\nthe backend (B4.6) so the dashboard can show per-recording upload status.")


export const UploadBacklogSchema = z.object({ "count": z.number().int().default(0), "oldest_pending_seconds": z.union([z.number().int(), z.null()]).default(null) }).describe("Upload-queue health gauges (V4.8): how many markers are pending and how\nold the oldest one is. The Slice 7 \">3h\" P2 alert lands against these.")


export const RecordingListItemSchema = z.object({ "recording_id": z.string(), "started_at": z.string(), "ended_at": z.union([z.string(), z.null()]).default(null), "duration_seconds": z.union([z.number().int(), z.null()]).default(null), "operator": z.union([z.string(), z.null()]).default(null), "flagged_for_upload": z.boolean().default(false), "upload_state": z.enum(["none","queued","uploading","uploaded","failed"]).describe("Where a recording is in the flag-for-upload pipeline (V4.6/V4.7).\n\n`none` — never flagged. `queued` — marker written, uploader not yet acting.\n`uploading` — transfer in progress. `uploaded` — in MinIO, marker deleted.\n`failed` — non-retryable failure; marker renamed `.failed`.").default("none") }).describe("One row of supervisor GET /recordings (S4.4) — what's on the Pi.\n\nA projection of RecordingMetadata sufficient for the dashboard list; segment\nURLs are decided by the backend (B4.2/B4.3), not enumerated here.")


export const StateResponseSchema = z.object({ "safety_state": z.enum(["INIT","IDLE","RUNNING","PAUSED","STOPPED","SHUTDOWN_ARM","SHUTDOWN_JETSON","DEGRADED"]).describe("Abbreviated supervisor safety state (architecture §2.4).\n\nSlice 3 only ever holds whatever the operator's last command implies; the\nreal transitions land in Slice 6. The full set is declared here so the\nenum doesn't have to change later.").default("IDLE"), "jetson": z.union([z.record(z.string(), z.any()), z.null()]).default(null), "plugs": z.record(z.string(), z.object({ "on": z.union([z.boolean(), z.null()]).default(null), "verified": z.boolean().default(false), "verified_at": z.union([z.string(), z.null()]).default(null) }).describe("A WiZ plug's last *verified* state (S3.1/S3.3).\n\n`on` reflects the state read back via getPilot after the last action or\nbackground refresh — not the last command issued. `verified` is False when\nthe most recent verify failed (the command may have been sent but could not\nbe confirmed); `verified_at` is the ISO-8601 timestamp of that read.")).optional(), "camera": z.object({ "up": z.union([z.boolean(), z.null()]).default(null) }).describe("Camera up/down, derived from the retained arm/video/state/* topics.").optional(), "recording": z.object({ "state": z.enum(["idle","starting","recording","stopping"]).describe("video's explicit-recording branch state machine (V4.3).\n\nSeparate from the live branch's LiveStateValue: the two branches share the\nencoder hardware but are independent. `idle` is the at-rest state (no\nrecording), mirroring the live branch's `off`.").default("idle"), "active_recording_id": z.union([z.string(), z.null()]).default(null), "started_at": z.union([z.string(), z.null()]).default(null) }).describe("Retained payload on arm/video/state/recording (V4.5).\n\nThe single source of truth for \"is a recording active right now\"; the\ndashboard derives its Start/Stop button from this (via supervisor /state).").optional(), "upload_backlog": z.object({ "count": z.number().int().default(0), "oldest_pending_seconds": z.union([z.number().int(), z.null()]).default(null) }).describe("Upload-queue health gauges (V4.8): how many markers are pending and how\nold the oldest one is. The Slice 7 \">3h\" P2 alert lands against these.").optional() }).describe("Body of supervisor GET /state and backend GET /api/state (S3.3).\n\n`jetson` is whatever the Jetson's GET /state returned (opaque to the\nbackend and dashboard — supervisor caches it and passes it through);\n`null` when the Jetson has not been reached yet.\n\nSlice 4 adds `recording` (the retained recording-state topic, cached) and\n`upload_backlog` (the V4.8 gauges, cached) so the dashboard's recording\ncontrols and backlog indicator read from the same /state poll as the rest.")


